Each python file is named after the learning algorithm that it tests.
The code is commented to show which potion of the code gives which results.
To generate those results, uncomment that portion of the code. 